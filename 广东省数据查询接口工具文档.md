# 广东省数据查询接口工具文档

生成日期：2026-06-11

本文档整理当前代码库中广东省相关的数据查询接口工具。主体以 `backend/app/tools/__init__.py` 中 `create_global_tool_registry()` 已注册的 LLM 工具为准；源码中存在但未注册的历史兼容工具放在附录。

## 1. 通用约定

### 1.1 调用形态

这些接口不是 HTTP 路由，而是 Agent/LLM 工具调用接口。工具名对应 `LLMTool.name`，入参对应各工具的 `function_schema.parameters`。

示例调用形态：

```json
{
  "tool": "query_gd_suncere_city_day",
  "arguments": {
    "cities": ["广州", "深圳"],
    "start_date": "2026-05-01",
    "end_date": "2026-05-31",
    "ns_type": 2
  }
}
```

### 1.2 常见返回结构

多数工具返回 UDF v2.0 风格结构：

```json
{
  "success": true,
  "status": "success",
  "data": [],
  "metadata": {
    "tool_name": "工具名",
    "schema_version": "v2.0",
    "data_id": "可选，数据注册ID",
    "report_data_id": "可选，统计报表数据注册ID"
  },
  "summary": "结果摘要"
}
```

大结果通常会写入 data registry，后续用 `read_data_registry` 读取 `data_id` 或 `report_data_id`。统计报表类工具一般优先读取结构化视图，例如 `reporting`、`cities`、`stations`、`province`、`raw`、`result`。

### 1.3 常用枚举

`ns_type`：

| 值 | 含义 |
| --- | --- |
| `2` | 新国标，HJ 633-2026 |
| `1` | 旧国标，HJ 633-2013 |

`data_type` / `data_source`：

| 值 | 含义 |
| --- | --- |
| `0` | 原始实况 |
| `1` | 审核实况 |
| `2` | 原始标况 |
| `3` | 审核标况 |

`sand_type`：

| 值 | 含义 |
| --- | --- |
| `0` | 不扣沙 |
| `1` | 扣沙，默认值 |

`time_type`：

| 值 | 含义 |
| --- | --- |
| `3` | 周报 |
| `4` | 月报 |
| `5` | 季报 |
| `7` | 年报 |
| `8` | 任意时间段 |

## 2. 广东联网空气质量接口工具

### 2.1 城市小时数据：`query_gd_suncere_city_hour`

注册状态：已注册

源码：`backend/app/tools/query/query_gd_suncere/tool_wrapper.py`

用途：查询广东省城市级小时空气质量数据，适合小时过程、区域传输、多城市小时对比。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `cities` | `string[]` | 城市名称列表 |
| `start_time` | `string` | 开始时间，`YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，`YYYY-MM-DD HH:MM:SS` |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `include_weather` | `boolean` | `true` | 是否包含气象字段 |
| `ns_type` | `integer` | `2` | `2` 新国标，`1` 旧国标 |
| `skip_count` | `integer` | `0` | 分页跳过数 |
| `max_result_count` | `integer` | `1000` | 最大返回条数 |

示例：

```json
{
  "cities": ["广州", "佛山"],
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-02 23:00:00",
  "include_weather": true,
  "ns_type": 2
}
```

### 2.2 城市日数据：`query_gd_suncere_city_day`

注册状态：已注册

源码：`backend/app/tools/query/query_gd_suncere/tool_wrapper.py`

用途：查询广东省城市级日空气质量数据，适合日变化、多城市日均对比、长时间序列分析。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `cities` | `string[]` | 城市名称列表 |
| `start_date` | `string` | 开始日期，`YYYY-MM-DD` |
| `end_date` | `string` | 结束日期，`YYYY-MM-DD` |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `data_type` | `integer` | 自动 | `0/1/2/3`，不传时近三天原始、三天外审核 |
| `sand_type` | `integer` | `1` | `0` 不扣沙，`1` 扣沙 |
| `ns_type` | `integer` | `2` | `2` 新国标，`1` 旧国标 |
| `cal_area_type` | `integer` | `0` | `0` 总站，`2` 省站，`3` 国控，`4` 国控+省控，`5` 国控+省控(除区域) |
| `skip_count` | `integer` | `0` | 分页跳过数 |
| `max_result_count` | `integer` | `1000` | 最大返回条数 |

示例：

```json
{
  "cities": ["广州", "深圳", "佛山"],
  "start_date": "2026-05-01",
  "end_date": "2026-05-31",
  "ns_type": 2,
  "sand_type": 1
}
```

### 2.3 站点小时数据：`query_gd_suncere_station_hour_new`

注册状态：已注册

源码：`backend/app/tools/query/query_gd_suncere/tool_wrapper.py`

用途：查询广东省站点级小时空气质量数据。用户指定具体站点，或需要城市下辖站点小时数据时使用。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `start_time` | `string` | 开始时间，`YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，`YYYY-MM-DD HH:MM:SS` |

约束：`cities` 与 `stations` 至少提供一个。为避免数据量过大，不支持默认全省站点查询。

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `cities` | `string[]` | 无 | 城市列表，工具自动展开站点 |
| `stations` | `string[]` | 无 | 站点名称列表或站点编码 |
| `station_type` | `string` | `国控` | 仅 `cities` 生效，常用 `国控`、`省控`、`市控` |
| `include_weather` | `boolean` | `true` | 是否包含气象字段 |
| `data_type` | `integer` | 自动 | `0/1/2/3` |
| `ns_type` | `integer` | `2` | `2` 新国标，`1` 旧国标 |
| `skip_count` | `integer` | `0` | 分页跳过数 |
| `max_result_count` | `integer` | `1000` | 最大返回条数 |

示例：

```json
{
  "cities": ["广州"],
  "station_type": "国控",
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-01 23:00:00",
  "include_weather": true
}
```

### 2.4 站点日数据：`query_gd_suncere_station_day_new`

注册状态：已注册

源码：`backend/app/tools/query/query_gd_suncere/tool_wrapper.py`

用途：查询广东省站点级日空气质量数据。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `start_date` | `string` | 开始日期，`YYYY-MM-DD` |
| `end_date` | `string` | 结束日期，`YYYY-MM-DD` |

约束：`cities` 与 `stations` 至少提供一个。为避免数据量过大，不支持默认全省站点查询。

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `cities` | `string[]` | 无 | 城市列表，工具自动展开站点 |
| `stations` | `string[]` | 无 | 站点名称列表或站点编码 |
| `station_type` | `string` | `国控` | 仅 `cities` 生效 |
| `data_type` | `integer` | 自动 | `0/1/2/3` |
| `sand_type` | `integer` | `1` | `0` 不扣沙，`1` 扣沙 |
| `ns_type` | `integer` | `2` | `2` 新国标，`1` 旧国标 |
| `skip_count` | `integer` | `0` | 分页跳过数 |
| `max_result_count` | `integer` | `1000` | 最大返回条数 |

示例：

```json
{
  "stations": ["麓湖"],
  "start_date": "2026-05-01",
  "end_date": "2026-05-31",
  "ns_type": 2
}
```

### 2.5 区县日数据：`query_gd_suncere_district_day`

注册状态：已注册

源码：`backend/app/tools/query/query_gd_suncere/tool_wrapper.py`

用途：查询广东省区县级日空气质量数据，适合区县排名、区县日均对比、城市内各区县过程分析。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `start_time` | `string` | 开始时间，`YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，`YYYY-MM-DD HH:MM:SS` |

约束：`districts`、`district_codes`、`cities` 至少提供一个。

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `districts` | `string[]` | 无 | 区县名称或9位区县编码 |
| `district_codes` | `string[]` | 无 | 9位区县编码 |
| `cities` | `string[]` | 无 | 城市名称或6位城市编码，提供后自动展开下辖区县 |
| `cal_area_type` | `integer` | `0` | `0` 国控+省控，`1` 省控，`2` 国控 |
| `data_type` | `integer` | `1` | `0/1/2/3` |
| `ns_type` | `integer` | `2` | `2` 新国标，`1` 旧国标 |
| `sand_type` | `integer` | `1` | `0` 不扣沙，`1` 扣沙 |
| `skip_count` | `integer` | `0` | 分页跳过数 |
| `max_result_count` | `integer` | `20` | 最大返回条数 |

示例：

```json
{
  "cities": ["清远"],
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-31 23:59:59",
  "cal_area_type": 0,
  "ns_type": 2
}
```

### 2.6 区县统计报表：`query_gd_suncere_district_report`

注册状态：已注册

源码：`backend/app/tools/query/query_gd_suncere/tool_wrapper.py`

用途：查询广东省区县统计报表，支持月度、年度、任意时段统计。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `start_time` | `string` | 开始时间，`YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，`YYYY-MM-DD HH:MM:SS` |

约束：`districts`、`district_codes`、`cities` 至少提供一个。

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `districts` | `string[]` | 无 | 区县名称或9位区县编码 |
| `district_codes` | `string[]` | 无 | 9位区县编码 |
| `cities` | `string[]` | 无 | 城市名称或6位城市编码，提供后自动展开下辖区县 |
| `time_type` | `integer` | `8` | `4` 月度，`7` 年度，`8` 任意时段 |
| `area_type` | `integer` | `1` | 固定区县口径 |
| `cal_area_type` | `integer` | `0` | `0` 国控+省控，`1` 省控，`2` 国控 |
| `data_source` | `integer` | `1` | `0` 原始实况，`1` 审核实况 |
| `ns_type` | `integer` | `2` | `2` 新国标，`1` 旧国标 |
| `sand_type` | `integer` | `1` | 扣沙类型 |
| `skip_count` | `integer` | `0` | 分页跳过数 |
| `max_result_count` | `integer` | `20` | 最大返回条数 |

示例：

```json
{
  "districts": ["清城区", "清新区"],
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-31 23:59:59",
  "time_type": 8,
  "ns_type": 2
}
```

### 2.7 区域小时对比：`query_gd_suncere_regional_comparison`

注册状态：已注册

源码：`backend/app/tools/query/query_gd_suncere/tool_wrapper.py`

用途：查询目标城市与周边城市小时数据，用于区域传输、本地生成与外部输送对比。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `target_city` | `string` | 目标城市名称 |
| `nearby_cities` | `string[]` | 周边城市名称列表 |
| `start_time` | `string` | 开始时间，`YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，`YYYY-MM-DD HH:MM:SS` |

示例：

```json
{
  "target_city": "广州",
  "nearby_cities": ["佛山", "东莞", "清远"],
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-03 23:00:00"
}
```

### 2.8 兼容对比报表：`query_gd_suncere_report_compare`

注册状态：已注册，但源码说明为兼容保留

源码：`backend/app/tools/query/query_gd_suncere/tool_wrapper.py`

用途：查询广东省对比分析报表数据，支持城市/区县/站点区域类型。城市正式同比/环比场景优先使用 `query_city_standard_yoy_report`。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `cities` | `string[]` | 城市名称列表 |
| `time_point` | `string[]` | 当前时间范围，`[start, end]` |
| `contrast_time` | `string[]` | 对比时间范围，`[start, end]` |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `time_type` | `integer` | `8` | `4` 月报，`8` 任意时间 |
| `area_type` | `integer` | `2` | `0` 站点，`1` 区县，`2` 城市 |
| `pollutant_codes` | `string[]` | 无 | 污染物字段过滤 |

示例：

```json
{
  "cities": ["广州", "深圳"],
  "time_point": ["2026-05-01 00:00:00", "2026-05-31 23:59:59"],
  "contrast_time": ["2025-05-01 00:00:00", "2025-05-31 23:59:59"],
  "time_type": 8,
  "area_type": 2
}
```

## 3. 正式统计报表接口工具

### 3.1 城市统计报表：`query_city_standard_report`

注册状态：已注册

源码：`backend/app/tools/query/query_city_standard_report/tool.py`

用途：查询广东省城市统计报表接口，直接使用联网接口返回的新/旧国标统计结果，不进行本地日报重算。适合综合指数、达标/超标天数、污染物统计浓度、首要污染物、排名等正式报表口径。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `start_time` | `string` | 开始时间，支持 `YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，支持 `YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS` |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `cities` | `string[]` | 接口默认范围 | 城市或区域名称；支持 `广东省`、`全省`、`珠三角`、`非珠三角`、`粤东西北`、`粤东`、`粤西`、`粤北` |
| `ns_type` | `integer` | 自动 | `2` 新国标，`1` 旧国标；跨 2025-01-01 自动拆分查询 |
| `pollutant_codes` | `string[]` | 无 | 接口字段过滤 |
| `plan_type` | `integer` | `0` | 接口 `planType` |
| `data_source` | `integer` | `1` | `0/1/2/3` |
| `sand_type` | `integer` | `1` | 扣沙类型 |
| `revise_type` | `integer` | `0` | 接口 `ReviseType` |
| `skip_count` | `integer` | `0` | 分页跳过数 |
| `max_result_count` | `integer` | `200` | 最大返回条数 |

注意：

- 2025-01-01 之前默认旧国标；2025-01-01 及之后默认新国标。
- 指定区域别名时，`data` 只返回对应区域汇总行；城市明细或原始记录可从 data registry 的 `reporting/raw/result/cities` 视图读取。
- 报告口径视图中 PM2.5 使用 `pM2_5_Decimal` 并保留1位小数。

示例：

```json
{
  "cities": ["广东省"],
  "start_time": "2026-05-01",
  "end_time": "2026-05-31",
  "ns_type": 2,
  "sand_type": 1
}
```

### 3.2 城市同比/环比统计报表：`query_city_standard_yoy_report`

注册状态：已注册

源码：`backend/app/tools/query/query_city_standard_report/tool.py`

用途：查询广东省城市同比/环比统计报表接口，直接返回当前值、对比值、增幅、排名等字段。适合同比、环比、双时段对比、改善/恶化分析。

时间参数二选一：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `time_point` | `string[]` | 当前时间范围，`[start, end]` |
| `contrast_time` | `string[]` | 对比时间范围，`[start, end]` |
| `query_period` | `object` | 兼容旧格式，包含 `start_date/end_date` |
| `comparison_period` | `object` | 兼容旧格式，包含 `start_date/end_date` |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `cities` | `string[]` | 接口默认范围 | 城市或区域名称，支持区域别名 |
| `ns_type` | `integer` | `2` | `2` 新国标，`1` 旧国标 |
| `pollutant_codes` | `string[]` | 无 | 接口字段过滤 |
| `plan_type` | `integer` | `0` | 接口 `planType` |
| `data_source` | `integer` | `1` | `0/1/2/3` |
| `sand_type` | `integer` | `1` | 扣沙类型 |
| `revise_type` | `integer` | `0` | 接口 `ReviseType` |
| `skip_count` | `integer` | `0` | 分页跳过数 |
| `max_result_count` | `integer` | `200` | 最大返回条数 |

示例：

```json
{
  "cities": ["广州", "深圳"],
  "time_point": ["2026-05-01", "2026-05-31"],
  "contrast_time": ["2025-05-01", "2025-05-31"],
  "ns_type": 2
}
```

### 3.3 站点统计报表：`query_station_standard_report`

注册状态：已注册

源码：`backend/app/tools/query/query_station_standard_report/tool.py`

用途：查询广东省站点统计报表接口，直接使用联网接口返回的新/旧国标统计结果，适合站点综合指数、达标/超标天数、污染物统计浓度、首要污染物、排名等报表。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `start_time` | `string` | 开始时间，支持 `YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，支持 `YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS` |

约束：`cities` 与 `stations` 至少提供一个。

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `cities` | `string[]` | 无 | 城市列表，按 `station_type` 自动映射为站点编码 |
| `stations` | `string[]` | 无 | 站点名称或站点编码 |
| `station_type` | `string` | `国控` | 仅 `cities` 生效 |
| `ns_type` | `integer` | 自动 | `2` 新国标，`1` 旧国标 |
| `data_source` | `integer` | `1` | `0/1/2/3` |
| `max_result_count` | `integer` | `200` | 最大返回条数 |

示例：

```json
{
  "cities": ["广州"],
  "station_type": "国控",
  "start_time": "2026-05-01",
  "end_time": "2026-05-31",
  "ns_type": 2
}
```

### 3.4 站点同比/环比统计报表：`query_station_standard_yoy_report`

注册状态：已注册

源码：`backend/app/tools/query/query_station_standard_report/tool.py`

用途：查询广东省站点同比/环比统计报表接口，直接返回当前值、对比值、增幅、排名等字段。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `time_point` | `string[]` | 当前时间范围，`[start, end]` |
| `contrast_time` | `string[]` | 对比时间范围，`[start, end]` |

约束：`cities` 与 `stations` 至少提供一个。也支持兼容参数 `query_period`、`comparison_period`。

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `cities` | `string[]` | 无 | 城市列表，按 `station_type` 自动映射为站点编码 |
| `stations` | `string[]` | 无 | 站点名称或站点编码 |
| `station_type` | `string` | `国控` | 仅 `cities` 生效 |
| `ns_type` | `integer` | 自动 | `2` 新国标，`1` 旧国标 |
| `data_source` | `integer` | `1` | `0/1/2/3` |
| `max_result_count` | `integer` | `200` | 最大返回条数 |

示例：

```json
{
  "stations": ["麓湖"],
  "time_point": ["2026-05-01", "2026-05-31"],
  "contrast_time": ["2025-05-01", "2025-05-31"],
  "ns_type": 2
}
```

## 4. 广东超站与组分数据接口工具

### 4.1 VOCs 组分数据：`get_vocs_data`

注册状态：已注册

源码：`backend/app/tools/query/get_vocs_data/tool_api.py`

用途：直接调用广东超站 API 查询 VOCs 组分数据，包括烷烃、烯烃、炔烃、芳香烃、OVOCs、卤代烃、有机硫等。适合 VOCs 组分分析、OFP 分析、臭氧溯源前置数据查询。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `start_time` | `string` | 开始时间，`YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，`YYYY-MM-DD HH:MM:SS` |

位置参数二选一：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `locations` | `string[]` | 城市或站点名称列表，自动映射站点编码，优先使用 |
| `station` + `code` | `string` | 中文站点名和站点编码，同时提供 |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `table_type` | `integer` | `1` | `1` 小时，`2` 日，`3` 月，`5` 年 |
| `data_type` | `integer` | `0` | `0` 原始，`1` 终审，`4` 初审，`5` 复审 |

示例：

```json
{
  "locations": ["清远"],
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-02 00:00:00",
  "table_type": 1,
  "data_type": 0
}
```

### 4.2 PM2.5 水溶性离子：`get_pm25_ionic`

注册状态：已注册

源码：`backend/app/tools/query/get_pm25_ionic/tool.py`

用途：查询 PM2.5 水溶性离子组分，例如 SO4、NO3、NH4、F、Cl、Na、K、Mg、Ca 等。适合 PMF 和二次气溶胶分析。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `start_time` | `string` | 开始时间，`YYYY-MM-DD HH:MM:SS` |
| `end_time` | `string` | 结束时间，`YYYY-MM-DD HH:MM:SS` |

位置参数二选一：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `locations` | `string[]` | 城市或站点名称列表，自动映射站点编码，优先使用 |
| `station` + `code` | `string` | 中文站点名和站点编码，同时提供 |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `data_type` | `integer` | `0` | `0` 原始，`1` 审核 |
| `time_type` | `integer` | `1` | `1` 小时，`2` 日，`3` 月，`5` 年 |

示例：

```json
{
  "locations": ["清远"],
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-02 00:00:00",
  "time_type": 1,
  "data_type": 0
}
```

### 4.3 PM2.5 碳组分：`get_pm25_carbon`

注册状态：已注册

源码：`backend/app/tools/query/get_pm25_carbon/tool.py`

用途：查询 PM2.5 碳组分 OC/EC，适合 PMF 源解析、二次有机气溶胶、EC/OC 比值分析。

必填参数与位置参数同 `get_pm25_ionic`。

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `data_type` | `integer` | `0` | `0` 原始，`1` 审核 |
| `time_granularity` | `integer` | `1` | `1` 小时，`2` 日，`3` 月，`5` 年；源码注释说明数值数据主要使用小时粒度 |

示例：

```json
{
  "locations": ["清远"],
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-02 00:00:00",
  "time_granularity": 1
}
```

### 4.4 PM2.5 地壳元素：`get_pm25_crustal`

注册状态：已注册

源码：`backend/app/tools/query/get_pm25_crustal/tool.py`

用途：查询 PM2.5 地壳元素，例如 Al、Si、Fe、Ca、Ti、K、Mn 等。适合扬尘源解析和地壳元素分析。

必填参数与位置参数同 `get_pm25_ionic`。

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `data_type` | `integer` | `1` | `0` 原始，`1` 审核；源码说明地壳元素数值需用审核数据 |
| `time_granularity` | `integer` | `0` | 固定 `0` |
| `elements` | `string[]` | `["Al","Si","Fe","Ca","Ti","Mn"]` | 元素列表 |

示例：

```json
{
  "locations": ["清远"],
  "start_time": "2026-05-01 00:00:00",
  "end_time": "2026-05-02 00:00:00",
  "elements": ["Al", "Si", "Fe", "Ca"]
}
```

## 5. 其他广东相关查询工具

### 5.1 站点 5 分钟数据：`get_5min_data`

注册状态：已注册

源码：`backend/app/tools/query/get_5min_data/tool.py`

用途：查询站点 5 分钟污染物浓度和气象数据。数据源为 SQL Server `air_quality_db`，表名格式 `Air_5m_{年份}_{站点代码}_Src`。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `station` | `string` | 站点名称或站点代码 |
| `start_time` | `string` | 开始时间，ISO 8601，如 `2026-01-01T00:00:00` |
| `end_time` | `string` | 结束时间，ISO 8601 |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `pollutants` | `string[]` | 全部 | 污染物或气象字段，如 `PM2.5`、`O3`、`WS`、`WD`、`TEMP`、`RH` |

限制：

- 不支持跨年查询。
- 需要目标站点对应年份的 5 分钟数据表存在。

示例：

```json
{
  "station": "1001A",
  "start_time": "2026-01-01T00:00:00",
  "end_time": "2026-01-02T00:00:00",
  "pollutants": ["PM2.5", "O3", "WS", "WD"]
}
```

### 5.2 附近站点查询：`get_nearby_stations`

注册状态：已注册

源码：`backend/app/tools/query/get_nearby_stations/tool.py`

用途：根据目标坐标和半径查询附近站点信息，返回距离和方向。

必填参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `target_lat` | `number` | 目标纬度 |
| `target_lon` | `number` | 目标经度 |

可选参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `radius_km` | `number` | `50.0` | 搜索半径，单位公里 |
| `station_type` | `string` | 无 | 站点类型过滤 |

注意：源码中实际查询逻辑仍为 TODO，注释预期调用广东省站点 API 或数据库；当前实现可能返回模拟数据。

示例：

```json
{
  "target_lat": 23.1291,
  "target_lon": 113.2644,
  "radius_km": 30
}
```

### 5.3 城市污染物排名分析：`analyze_city_pollutant_rankings`

注册状态：已注册

源码：`backend/app/tools/query/city_pollutant_rankings/tool.py`

用途：对 `query_city_standard_report` 或 `query_city_standard_yoy_report` 返回的城市报表数据做确定性排名，适合空气质量通报中的较好/较差城市、PM2.5/PM10/O3 浓度较低/较高城市排名。

参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `data_id` | `string` | 报表工具返回的 `report_data_id` 或 `data_id`，优先使用 |
| `view` | `string` | 读取 data registry 的视图 |
| `records` | `object[]` | 也可直接传记录列表 |
| `pollutants` | `string[]` | 排名污染物列表 |
| `top_n` | `integer` | 返回前 N 名 |

示例：

```json
{
  "data_id": "report_xxx",
  "pollutants": ["PM2.5", "PM10", "O3"],
  "top_n": 5
}
```

## 6. 工具选择建议

| 需求 | 推荐工具 |
| --- | --- |
| 城市日均原始/审核数据 | `query_gd_suncere_city_day` |
| 城市小时过程 | `query_gd_suncere_city_hour` |
| 站点小时过程 | `query_gd_suncere_station_hour_new` |
| 站点日均数据 | `query_gd_suncere_station_day_new` |
| 区县日均数据 | `query_gd_suncere_district_day` |
| 城市正式统计报表、通报核数 | `query_city_standard_report` |
| 城市同比/环比正式报表 | `query_city_standard_yoy_report` |
| 站点正式统计报表 | `query_station_standard_report` |
| 站点同比/环比正式报表 | `query_station_standard_yoy_report` |
| 区县统计报表 | `query_gd_suncere_district_report` |
| 区域传输小时对比 | `query_gd_suncere_regional_comparison` |
| VOCs 组分 | `get_vocs_data` |
| PM2.5 离子 | `get_pm25_ionic` |
| PM2.5 OC/EC | `get_pm25_carbon` |
| PM2.5 地壳元素 | `get_pm25_crustal` |
| 5分钟站点数据 | `get_5min_data` |
| 城市污染物排名 | `analyze_city_pollutant_rankings` |

## 7. 已发现的未注册或兼容工具

以下工具类在源码中存在，但当前未在 `backend/app/tools/__init__.py` 的全局注册表中注册，默认不可通过全局工具注册表调用。除非补充注册或由其他路径显式实例化，否则不应作为首选接口写入业务调用。

| 工具名 | 源码位置 | 状态说明 | 替代建议 |
| --- | --- | --- | --- |
| `query_gd_suncere_report` | `backend/app/tools/query/query_gd_suncere/tool_wrapper.py` | 兼容保留类，未注册 | 城市正式统计报表用 `query_city_standard_report` |
| `query_standard_comparison` | `backend/app/tools/query/query_gd_suncere/tool_wrapper.py` | 未注册 | 如需新旧标准同段对比，先确认是否需要注册该工具 |
| `query_gd_suncere_city_day_old_standard` | `backend/app/tools/query/query_gd_suncere/tool_wrapper.py` | 未注册 | 日数据旧标准可用 `query_gd_suncere_city_day` 并传 `ns_type=1` |
| `query_old_standard_report` | `backend/app/tools/query/query_gd_suncere/tool_wrapper.py` | 未注册 | 正式报表可用 `query_city_standard_report` 并传 `ns_type=1` |
| `query_new_standard_report` | `backend/app/tools/query/query_new_standard_report/tool.py` | 未注册 | 正式报表优先用 `query_city_standard_report` 并传 `ns_type=2` |
| `query_station_new_standard_report` | `backend/app/tools/query/query_station_new_standard_report/tool.py` | 未注册 | 站点正式报表用 `query_station_standard_report` 并传 `ns_type=2` |
| `compare_standard_reports` | `backend/app/tools/query/compare_standard_reports/tool.py` | 未注册 | 城市同比/环比用 `query_city_standard_yoy_report` 并传 `ns_type=2` |
| `compare_old_standard_reports` | `backend/app/tools/query/compare_old_standard_reports/tool.py` | 未注册 | 城市同比/环比用 `query_city_standard_yoy_report` 并传 `ns_type=1` |
| `compare_station_standard_reports` | `backend/app/tools/query/compare_station_standard_reports/tool.py` | 未注册 | 站点同比/环比用 `query_station_standard_yoy_report` |
| `get_pm25_component` / `get_pm25_recovery` | `backend/app/tools/query/get_pm25_component/tool.py` | 未注册，部分实现偏参数生成/占位 | 结构化组分查询优先用 `get_pm25_ionic`、`get_pm25_carbon`、`get_pm25_crustal` |
| `get_carbon_data` | `backend/app/tools/query/get_carbon_data/tool.py` | 未注册，自然语言接口 | 结构化 OC/EC 查询优先用 `get_pm25_carbon` |

## 8. 源码核对范围

本次文档主要核对以下文件：

- `backend/app/tools/__init__.py`
- `backend/app/tools/query/query_gd_suncere/tool_wrapper.py`
- `backend/app/tools/query/query_city_standard_report/tool.py`
- `backend/app/tools/query/query_station_standard_report/tool.py`
- `backend/app/tools/query/get_vocs_data/tool_api.py`
- `backend/app/tools/query/get_pm25_ionic/tool.py`
- `backend/app/tools/query/get_pm25_carbon/tool.py`
- `backend/app/tools/query/get_pm25_crustal/tool.py`
- `backend/app/tools/query/get_5min_data/tool.py`
- `backend/app/tools/query/get_nearby_stations/tool.py`
- `backend/app/tools/query/city_pollutant_rankings/tool.py`
- `backend/app/tools/query/query_new_standard_report/tool.py`
- `backend/app/tools/query/query_station_new_standard_report/tool.py`
- `backend/app/tools/query/compare_standard_reports/tool.py`
- `backend/app/tools/query/compare_old_standard_reports/tool.py`
- `backend/app/tools/query/compare_station_standard_reports/tool.py`
