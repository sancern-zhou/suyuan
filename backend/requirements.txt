# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
sse-starlette==2.4.1
pydantic==2.12.5
pydantic-settings==2.6.0

# HTTP Client
requests==2.32.5
urllib3>=1.21.1,<3
chardet>=3.0.2,<6
charset-normalizer>=2,<4
httpx==0.27.2
socksio>=1.0.0
aiohttp==3.10.11

# Data Processing
pandas==2.2.3
numpy==1.26.3  # Keep aligned with environment.yml; NumPy 2.x removes np.mat used by nimfa.
xarray>=2024.1.0
netCDF4>=1.6.5
scipy>=1.10.0  # Sparse matrix optimization for OBM calculations
PyKrige>=1.7.2  # Kriging interpolation backend for GIS concentration surfaces
nimfa>=1.0.0   # Non-negative matrix factorization for PMF dual-mode
defusedxml>=0.7.1  # Safe XML parsing (protects against XML attacks)

# Visualization
matplotlib==3.9.2
cartopy==0.23.0  # For trajectory analysis and visualization

# LLM Integration
openai==1.54.4
anthropic==0.39.0
tiktoken>=0.8.0  # Token counting for LLM monitoring
json-repair>=0.57.0  # Automatically repair malformed JSON from LLMs
llama-index-core>=0.14.22,<0.15  # Optional cognitive map schema-guided extraction provider

# Async & Concurrency
celery==5.4.0
redis==5.2.0
nacos-sdk-python==2.0.11

# Environment & Config
python-dotenv==1.0.1

# Logging & Monitoring
structlog==24.4.0

# Date & Time
python-dateutil==2.9.0

# CORS
fastapi-cors==0.0.6

# Optional: Rate Limiting
slowapi==0.1.9

# Runtime dependencies (single source of truth; dev tools live in requirements-dev.txt)

# SQL Server Database Driver
pyodbc==5.2.0
aioodbc==0.5.0

# PostgreSQL & TimescaleDB (for meteorological data storage)
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.35
alembic==1.13.3

# Scheduling
apscheduler==3.10.4

# Vector Database (for Agent long-term memory)
qdrant-client==1.12.0

# Embeddings (for vector search)
sentence-transformers==3.1.1
jieba>=0.42.1  # Chinese word segmentation for BM25 sparse vectors
rapidfuzz>=3.14,<4  # Fast deterministic near-duplicate checks for exam generation

# Report Export (PDF/Word generation)
weasyprint==63.1
python-docx==1.1.2
python-pptx==1.0.2
openpyxl>=3.1.0  # Excel file manipulation for Office tools
pypdf>=5.0.0  # PDF processing for Office document preview
PyMuPDF==1.26.7  # PDF rendering, font inspection, and OCR preprocessing
pdfplumber==0.11.9  # PDF text and table extraction
pdfminer.six==20251230  # Fast PDF text-layer extraction used by Unstructured and pdfplumber
pdf2image==1.17.0  # PDF page rasterization through Poppler
pytesseract==0.3.13  # OCR integration through the Tesseract executable
Pillow==12.0.0  # Image processing for Office, reports, OCR, and visualizations
unstructured==0.22.32  # Knowledge-base parsing for non-PDF document formats
unstructured-client==0.32.4  # Keep Unstructured compatible with the pinned HTTP/Pydantic stack
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl  # Offline sentence splitting for Unstructured
markdown==3.7
jinja2==3.1.4
beautifulsoup4==4.14.3  # HTML parsing for reports, knowledge base, and query tools

# File Upload & Async I/O
python-multipart==0.0.21  # FastAPI UploadFile/Form request parsing
aiofiles==24.1.0  # Asynchronous knowledge-base import/export

# Browser Automation (for browser tool)
playwright==1.48.0  # Cross-platform browser automation

# Machine Learning
scikit-learn==1.8.0  # Regression and prediction analysis tools

# Optional: Python code execution (for execute_python tool)
ipython>=8.0.0  # Optional, for better output capture and error handling

# Social Platform Integration (QQ, WeChat, DingTalk, WeCom)
qq-botpy>=1.0.0  # QQ robot SDK
qrcode>=8.0  # QR code generation for WeChat login
pycryptodome>=3.20.0  # Cryptographic library for WeChat
dingtalk-stream>=0.24.0  # DingTalk streaming SDK
wecom-aibot-sdk-python>=0.1.5  # WeCom (WeChat Work) SDK
pyyaml>=6.0  # YAML configuration parsing

# Search APIs (for web_search tool)
tencentcloud-sdk-python>=3.0.0  # Tencent Cloud WSA API (optional, for enhanced Chinese search)
