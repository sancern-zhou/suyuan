# Long Label Rules

Long labels are a primary cause of unreadable report charts.

- Prefer `horizontal_bar` for long category labels.
- Wrap labels at stable character boundaries when possible.
- Use numbered labels when full labels cannot fit.
- Return mapping-table data when labels are abbreviated or numbered.
- Do not rotate long labels into a dense unreadable block.
